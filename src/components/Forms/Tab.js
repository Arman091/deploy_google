import { styled, Tabs,Paper,Tab } from "@mui/material";
const useStyles = styled(({ theme }) => ({
  root: {
    flexGrow: 1,
  },
  tab: {
    fontSize: 12,
    color: "#5f6368",
    textTransform: "capitalize",
    height: 10,
    fontWeight: "600",
    fontFamily: "Google Sans, Roboto, Arial, sans-serif",
  },
  tabs: {
    height: 10,
  },
}))();

function FormTabs() {
    
}

export default FormTabs;